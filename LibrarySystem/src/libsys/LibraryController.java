package libsys;

public class LibraryController {
	
	/**
	 * Creates a Customer object with the given username and password.
	 * 
	 * @param username the username for the new Customer
	 * @param password the password for the new Customer
	 * @return true if the Customer object is successfully created, false if not
	 */
	public boolean createCustomer(String username, String password) {
		//TODO
		return false;
	}
	
	/**
	 * Creates a Librarian object with the given username and password.
	 * 
	 * @param username the username for the new Librarian
	 * @param password the password for the new Librarian
	 * @return
	 */
	public boolean createLibrarian(String username, String password) {
		//TODO
		return false;
	}
	
	/**
	 * Converts the Customer with the given username into a Librarian.
	 * 
	 * @param username the username of the Customer to be upgraded
	 * @return ture if the Customer is successfully upgraded, false if not
	 */
	public boolean upgradeCustomer(String username) {
		//TODO
		return false;
	}
	
	/**
	 * Logs the user into the Customer account with the given username.
	 * 
	 * @param username the username of the Customer account to log in to
	 * @param password the password for the Customer account
	 * @return true if the Customer is successfully logged in, false if not
	 */
	public boolean loginCustomer(String username, String password) {
		//TODO
		return false;
	}
	
	/**
	 * Logs the user into the Librarian account with the given username.
	 * 
	 * @param username the username of the Librarian account to log in to
	 * @param password the password for the Librarian account
	 * @return true if the Librarian is successfully logged in, false if not
	 */
	public boolean loginLibrarian(String username, String password) {
		//TODO
		return false;
	}
	
}
